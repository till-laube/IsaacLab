# Gripper session configuration
# force: 0-255 (Robotiq force setting)
# current_limit: Amps threshold to stop closing (null = full grip)

left_gripper:
  force: 75
  current_limit: 0.4

right_gripper:
  force: 140
  current_limit: 0.7
